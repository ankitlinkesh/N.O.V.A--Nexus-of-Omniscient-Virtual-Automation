"""MCP (Model Context Protocol) client subsystem.

Inert by default: nothing here connects to any process or network endpoint
unless EVA_MCP_ENABLED is truthy AND servers are configured (see config.py).
"""
